package interfacesAndAbstracClasses;

public interface DateUtilsInterface {
    boolean isValidDate(int year, int month, int day);
    String isValidDate(String date);
    boolean isValidDate(int day);
}
